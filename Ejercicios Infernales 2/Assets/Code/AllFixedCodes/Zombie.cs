using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Zombie : BehaviorsSystem
{
    // public GameObject target;     
    // Vector3 targetPos;
    // Vector3 steering;


    // Update is called once per frame
    void Update()
    {
        // steering = this.Seek(target.transform.position);        
        // Move(steering + avoidForce);
        // boxCollider.offset = new Vector2(this.maxSeeAhead,0);
    }
    // void Move(Vector3 steering)
    // {
    //     this.speed += steering;
    //     transform.position += this._velocity * Time.deltaTime;
    // }
}
